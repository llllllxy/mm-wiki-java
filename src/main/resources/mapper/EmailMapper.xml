<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.tinycloud.mmwiki.mapper.EmailMapper">

    <select id="findAll" resultType="org.tinycloud.mmwiki.domain.EmailServer">
        select email_id,
               name,
               sender_address,
               sender_name,
               sender_title_prefix,
               host,
               port,
               username,
               password,
               is_ssl,
               is_used,
               create_time,
               update_time
        from mw_email
        order by email_id desc
    </select>

    <select id="findByNameLike" resultType="org.tinycloud.mmwiki.domain.EmailServer">
        select email_id,
               name,
               sender_address,
               sender_name,
               sender_title_prefix,
               host,
               port,
               username,
               password,
               is_ssl,
               is_used,
               create_time,
               update_time
        from mw_email
        where name like concat('%', #{keyword}, '%')
        order by email_id desc
    </select>

    <select id="findById" resultType="org.tinycloud.mmwiki.domain.EmailServer">
        select email_id,
               name,
               sender_address,
               sender_name,
               sender_title_prefix,
               host,
               port,
               username,
               password,
               is_ssl,
               is_used,
               create_time,
               update_time
        from mw_email
        where email_id = #{emailId} limit 1
    </select>

    <select id="countByName" resultType="long">
        select count(*)
        from mw_email
        where name = #{name}
    </select>

    <select id="countByNameAndNotId" resultType="long">
        select count(*)
        from mw_email
        where name = #{name}
          and email_id &lt;&gt; #{emailId}
    </select>

    <select id="findUsed" resultType="org.tinycloud.mmwiki.domain.EmailServer">
        select email_id,
               name,
               sender_address,
               sender_name,
               sender_title_prefix,
               host,
               port,
               username,
               password,
               is_ssl,
               is_used,
               create_time,
               update_time
        from mw_email
        where is_used = 1 limit 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="emailId">
        insert into mw_email(name, sender_address, sender_name, sender_title_prefix, host, port, username, password,
                             is_ssl, is_used, create_time, update_time)
        values (#{name}, #{senderAddress}, #{senderName}, #{senderTitlePrefix}, #{host}, #{port}, #{username},
                #{password}, #{isSsl}, #{isUsed}, #{createTime}, #{updateTime})
    </insert>

    <update id="update">
        update mw_email
        set name                = #{name},
            sender_address      = #{senderAddress},
            sender_name         = #{senderName},
            sender_title_prefix = #{senderTitlePrefix},
            host                = #{host},
            port                = #{port},
            username            = #{username},
            password            = #{password},
            is_ssl              = #{isSsl},
            update_time         = #{updateTime}
        where email_id = #{emailId}
    </update>

    <update id="clearUsed">
        update mw_email
        set is_used = 0
    </update>

    <update id="markUsed">
        update mw_email
        set is_used     = 1,
            update_time = now()
        where email_id = #{emailId}
    </update>

    <update id="deleteById">
        delete
        from mw_email
        where email_id = #{emailId}
    </update>

</mapper>
